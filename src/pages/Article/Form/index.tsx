import React from 'react';

import {
  useShallowReactive,
  useRef,
  onMounted,
  useReactive,
  useComputed,
} from '@/veact';
import { Table, Button, Card, Input, Divider, Spin, Modal, Space } from 'antd';

import styles from './style.module.less';

export const ArticleTest: React.FC = () => {
  return <span>dasdsaad</span>;
};
